import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';
import { TeamsResolver } from './teams.resolver';

@NgModule({
  declarations: [],
  imports: [CommonModule],
  providers: [TeamsResolver]
})
export class TeamsResolverModule {}
